---
emails:
    - templateKey: firstTemplateKey
      name: "First template"
      defaultSubjectTemplate: "Default subject 1"
      templatePath: uaa/emails/en/firstTemplateKey.ftl
      defaultContext: |
          {
              "name": "Default name"
          }
      contextSpec: |
          {
              "type": "object",
              "properties": {
                  "name": { "type": "string" }
              }
          }
      contextForm: |
          {
            "form": [
               { "key": "name", "title": { "en": "Name" }, "type": "string" }
            ]
          }
    - templateKey: secondTemplateKey
      name: "Second template"
      defaultSubjectTemplate: "Default subject 2"
      templatePath: uaa/emails/en/secondTemplateKey.ftl
      defaultContext: |
          {
              "title": "Default title"
          }
      contextSpec: |
          {
              "type": "object",
              "properties": {
                  "title": { "type": "string" }
              }
          }
      contextForm: |
          {
            "form": [
               { "key": "title", "title": { "en": "Title" }, "type": "string" }
            ]
          }
